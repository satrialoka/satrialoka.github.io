---
layout: post
title:  "Hello Word !"
date:   2017-05-11 22:13:44 +0700
categories: journal
---

Hello world,

This is my first post, i will write anything i like here, especially about machine learning and image processing. It's been a long time since i planned to write a blogs, and just until recently i've had the chance to make it happen.  

#$$e^{i\pi} + 1 = 0$$

#{% highlight python %}
#{% raw %}
#def dice_loss(pred, target, smooth = 1.):
#    pred = pred.contiguous()
#    target = target.contiguous()    
#    intersection = (pred * target).sum(dim=2).sum(dim=2)
#    loss = (1 - ((2. * intersection + smooth) / (pred.sum(dim=2).sum(dim=2) + target.sum(dim=2).sum(dim=2) + smooth)))
#    return loss.mean()
#{% endraw %}
#{% endhighlight %}